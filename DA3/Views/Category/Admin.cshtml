@{
    ViewData["Title"] = "Quản lỳ loại sản phẩm";
    Layout = "_Layout";
}
@model CategoryViewModel

<!DOCTYPE html>
<html lang="en">

<head>
</head>

<body>
    <div class="page-wrapper">
        <main class="main">
            <div class="page-content">
                <div class="cart">
                    <div class="container">
                        <div class="form-footer">
                            <button type="submit" onclick="location.href='@Url.Action("Create", "Category")'" class="btn btn-outline-primary-2">
                                <span>Thêm mới</span>
                                <i class="icon-long-arrow-right"></i>
                            </button>
                        </div><!-- End .form-footer -->
                        <div class="row">
                            <div class="col-lg-9">
                                <table class="table table-cart table-mobile">
                                    <thead>
                                        <tr>
                                            <th><b>Tên loại sản phẩm</b></th>
                                            <th><b>Mô tả</b></th>
                                            <th></th>
                                        </tr>
                                    </thead>

                                    <tbody>
                                        @foreach (var item in @Model?.Categories)
                                        {
                                            <tr>
                                                <td class="price-col">@item?.Name</td>
                                                <td class="price-col">@item?.Describe</td>
                                                <td class="remove-col">
                                                    <button class="btn-remove" onclick="location.href='@Url.Action("Edit", "Category", new {categoryId = @item.Id})'"><i class="icon-edit"></i></button>
                                                    <button class="btn-remove" onclick="location.href='@Url.Action("Remove", "Category", new {categoryId = @item.Id})'"><i class="icon-close"></i></button>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table><!-- End .table table-wishlist -->
                            </div><!-- End .col-lg-9 -->
                        </div><!-- End .row -->
                    </div><!-- End .container -->
                </div><!-- End .cart -->
            </div><!-- End .page-content -->
        </main><!-- End .main -->
    </div>
</body>
</html>